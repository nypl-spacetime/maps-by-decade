import styled from 'styled-components'

export const Button = styled.a`
  font-size: 80%;
  margin: 0 0.3em;
  opacity: 0.9;
  text-decoration: underline;
  color: white;

  &:visited {
    color: white;
  }
`
